#include <stdio.h>

#include <stdio.h>

void help(void) {
    printf(
        "Использование: dulehttpd [ОПЦИИ]\n\n"
        "Описание:\n"
        "  Комфортный и легкий HTTP-сервер. Позволяет тонко настроить порты, \n"
        "  ограничения по размеру файлов и доступ к ресурсам.\n\n"
        "Параметры:\n"
        "  -do,  --debug-output <0|1|2>\n"
        "        Уровень вывода отладочной информации (default: 2)\n"
        "          0 — выключен\n"
        "          1 — краткая информация\n"
        "          2 — полный отладочный вывод\n\n"
        "  -p,   --port <PORT>\n"
        "        Порт, на котором запустится сервер (default: 8080)\n\n"
        "  -la,  --listen-address <ADDR>\n"
        "        IP-адрес для прослушивания (default: 127.0.0.1)\n\n"
        "  -rd,  --root-directory <PATH>\n"
        "        Корневая директория с файлами сайта (default: /var/www/)\n"
        "        Важно: путь не должен содержать тильду (~)\n\n"
        "  -mc,  --max-connections <NUM>\n"
        "        Максимальное количество одновременных подключений (default: 5)\n\n"
        "  -fd,  --forbidden-directories <DIR1///DIR2...>\n"
        "        Директории, доступ к которым запрещен (возвращает 403 Forbidden).\n"
        "        Пути разделяются через '///'\n\n"
        "  -ff,  --forbidden-files <FILE1///FILE2...>\n"
        "        Файлы, доступ к которым запрещен (возвращает 403 Forbidden).\n"
        "        Пути разделяются через '///'\n\n"
        "  -mbs, --max-body-size <BYTES>\n"
        "        Максимальный размер тела запроса в байтах (default: 10240 / 10 KB)\n\n"
        "  -mis, --max-incoming-file-size <BYTES>\n"
        "        Максимальный размер загружаемого файла в байтах (default: 10240 / 10 KB)\n\n"
        "  -404, --file-at-404 <PATH>\n"
        "        Кастомный файл для ошибки 404 Not Found (default: встроенный dulehttpd 404)\n\n"
        "  -403, --file-at-403 <PATH>\n"
        "        Кастомный файл для ошибки 403 Forbidden (default: встроенный dulehttpd 403)\n\n"
        "  -h,   --help\n"
        "        Показать это справочное сообщение"
    );
}